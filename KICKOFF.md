# 首个任务

先读 `CLAUDE.md`，再读 `data/constants.json`。硬约束一节不要推翻。

## 目标

在写任何应用代码之前，先打通数据管道。**不要先搭 UI。**

## 步骤

### 1. 取一份上游 xlsx

从 https://www.h9dh.cn/ 下载 **破竹鸢 110阶竞速轴属性毕业率进阶计算器 2.5**（当前最新、版本号最高）。放到 `upstream/` 下，文件名保留原始版本号。

### 2. 拆解结构

用 openpyxl / SheetJS 打开，产出一份 `docs/xlsx-anatomy.md`，回答：

- 有哪些工作表？各自职责？
- 「期望」表的布局：`CLAUDE.md` 提到参考实现里 `B2:E7` 是装备槽区域、`B8:B21` 是一列标量输入、`C22/C23` 是双武器增效、`C24` 全武增、`C25/C26` 单体/群体奇术、`E18` 首领增伤、`E25` 蓄力技定音、`G1` 元素、`G2` 套装。核实这个映射对不对
- 公式用了哪些函数？有没有 codegen 难处理的（跨表引用、数组公式、命名区间、VLOOKUP 到隐藏表、迭代计算）
- 哪些单元格是输入，哪些是中间量，哪些是最终输出

### 3. 评估 codegen 可行性

给出结论：xlsx → JS 的转译，是「解析公式 AST 自己实现求值」还是「直接嵌一个现成的公式引擎（如 HyperFormula / formulajs）」更划算。带工作量估计。

**这一步的产出是判断，不是代码。** 先让我看结论再决定往下走。

### 4. 验证 constants.json

用 xlsx 里的实际数据核对 `data/constants.json`：满值表对不对、承音系数 0.94 是否成立、`_warning` 标注的三处异常（裂石威字段缺失、鸣金虹心法不一致、牵丝霖 baseline 数量级）到底是什么情况。发现出入直接改文件并在 commit message 里说明。

## 不要做的事

- 不要手写伤害公式
- 不要开始做 OCR 或任何 UI
- 不要去爬 wiki
- 不要动游戏客户端

## 交付

`docs/xlsx-anatomy.md` + 第 3 步的结论 + `data/constants.json` 的修订。停在这里等我确认。
