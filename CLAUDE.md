# Temper

《燕云十六声》配装 / 毕业率计算工具。纯静态网页，无后端。

命名取自 equal temperament（平均律）与金工「回火」双关 —— 游戏装备系统整套是音律隐喻：调律、定音、承音、转律、叠音。

---

## 一、这个项目的核心认知

**上游数据源是社区维护的 Excel 计算器，不是任何 wiki。**

这一点是整个项目的地基，已经过实地核查（见「已排除的路径」）。所有数据管道设计都必须围绕这个前提。

### 上游：h9dh.cn

社区计算器分发站，一个流派一个 `.xlsx`，带独立版本号。截至 2026-08-14 的文件清单：

| 流派 | 版本 | 更新日期 |
|---|---|---|
| 破竹鸢 | 2.5 | 2026-08-12 |
| 裂石威 | 2.3 | 2026-08-12 |
| 牵丝翊 | 2.1 | 2026-08-12 |
| 破竹尘 | 2.1 | 2026-08-12 |
| 裂石钧 | 2.1 | 2026-08-12 |
| 鸣金虹 | 1.3 | 2026-07-15 |
| 鸣金影 | 1.5 | 2026-07-09 |
| 牵丝玉 | 1.4 | 2026-07-09 |
| 破竹风 | 1.1 | 2026-07-09 |
| 牵丝霖 | 1.1 | 2026-07-09 |
| DIY 计算器 | 0.9.7 | 2026-08-08 |

站点公告原文：「表格需要等有轴才能更新/部分流派已更新」。**数值绑定竞速轴** —— 社区跑出新轴才重算，所以各流派版本号不同步是正常状态，不是数据缺失。

### 参考实现：yysls.leoq7.com

同类工具，已验证的架构（从其资源文件名与 codegen 注释推得）：

```
tesseract.min.js              OCR
excel-runtime.js              Excel 公式运行时
generated-calc-strings.js     由 xlsx 生成
generated-calc-metadata.js    由 xlsx 生成
generated-best40-stats.js
```

`generated-calc-metadata.js` 首行注释：`// Generated by scripts/excel_codegen/generate_metadata.py`

**它不自己实现伤害公式，而是把社区 xlsx 转译成 JS 在浏览器里跑。** Temper 采用同一思路。

---

## 二、硬约束

这些是已经论证过的决策，不要在实现中推翻：

1. **不手写伤害公式。** 走 codegen：xlsx → 中间表示 → 运行时求值。公式的维护权留在社区表格里，版本更新时重跑 codegen。手写公式意味着每次社区更新都要人工追数值，必然腐烂。

2. **承音值 = 满值 × 0.94，不存第二张表。** 已逐条核对全部 33 个词条，比值统一为 0.94（二位小数取整）。

3. **不爬 wiki。** 两个 wiki 都已实测出局，别再花时间。

4. **不碰游戏客户端。** 读资源文件 / 读内存 / 抓封包三条路都不走 —— 除工作量外，直接触碰网易反作弊与用户协议，账号风险实打实。OCR 读用户自己的屏幕是唯一干净方案。

5. **输入走 paste 事件。** Windows 玩家 `Win+Shift+S` 截图直接进剪贴板，网页监听 `paste` 即可拿到 Blob。不做上传按钮，不做文件选择。

6. **OCR 当模糊输入，不当精确识别。**
   - 词条名是封闭集合（约 33 项）→ 归一化后按编辑距离匹配到合法枚举，取最优；置信度低的高亮，提供下拉框修正
   - 数值按该词条的合法区间校验，超界即判定误识别并标红
   - 每个字段都可手动编辑。OCR 的价值是把输入从五分钟压到三十秒，不是全自动

7. **无后端。** IndexedDB 存本地装备库。无账号系统、无服务器、无数据合规负担。

---

## 三、求解器

搜索空间极小，别过度设计：

```
左4件：遍历所有套装，每个部位在该套装内取评分最高
右4件：同上
弓、玦：各自取最高
```

双层循环，毫秒级。**难点 100% 在评分函数**，也就是给定一套词条算出该流派的期望伤害 —— 这正是 codegen 要从 xlsx 里搬过来的东西。

毕业率 = 当前配装期望伤害 / 该流派 baseline（见 `data/constants.json` 的 `flows[].baseline`）。

---

## 四、已排除的路径（不要重走）

**灰机 wiki（yy16s.huijiwiki.com）** —— 已用 MediaWiki API 挖到底：
- 装了 SemanticMediaWiki 4.1.2，但属性命名空间只有 5 个默认属性（`Foaf:name` 等）+ 一个 `Organization`，社区根本没用 SMW 标数据
- 全站 1828 条目，内容为剧情、书信、NPC、时装外观子页面（`一人心/上衣` 这类）
- 前缀查 `调律` / `装备` / `飞隼` 全部零结果
- 武学页面（抽查「九曲惊神枪」）PvE 章节是三个空标题
- CirrusSearch 后端故障中

**BWIKI（wiki.biligame.com/yysls）** —— 2025-12 建站，128 页，首页大量 `我是页面名，请替换我` 占位符，装备/武学数据零条。

**官方数据小工具（yysls.cn / wherewindsmeetgame.com H5）** —— 登录才有，且只给个人历程数据与评分榜，不是图鉴。

**藏宝阁（yysls-m.cbg.163.com）** —— 数据最真但明确违反 ToS，风控严，不碰。

---

## 五、数据的时效性

- `data/constants.json` 抓取于 **2026-08-14**，对应 **110 阶**版本
- 词条满值随等级阶数变化，版本更新必须重新校准
- 五维换算系数（1体=60血、1敏=0.9最小攻+0.076%会心 等）流传的是 **96 版本**数据，**不要直接用于 110 阶**，需重新验证
- 流派 meta 随版本漂移，权重靠社区攻略跟进，任何爬虫都拿不到

时效性标注写进数据文件的 `_meta`，UI 上要显示「数据版本 / 抓取时间」，避免用户拿旧数据做决策。
